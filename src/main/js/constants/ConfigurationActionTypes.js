var mirrorToPath = require('../helpers/path-mirror.js');

var types = mirrorToPath({
  
  UPLOAD_FILE_REQUEST: null,
  UPLOAD_FILE_RESPONSE: null,
  SUBMIT_CONFIGURATION_PATH: null

});

module.exports = types;